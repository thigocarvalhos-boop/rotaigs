{
  "version": 2,
  "buildCommand": "npx prisma@6.4.1 generate && npx prisma@6.4.1 migrate deploy && npx vite build",
  "outputDirectory": "dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
